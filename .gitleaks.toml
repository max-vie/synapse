title = "Synapse gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "Allow documented placeholder values, not real generated secrets"
regexes = [
  '''replace-with-generated-password''',
  '''replace-with-generated-32-plus-character-secret''',
  '''replace-after-wikijs-admin-setup''',
]
